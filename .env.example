# Copy to .env. The tools load only this repository-local file.
XLEROBOT_CONFIG=config/local.yaml

# Required by the ACT /predict and /reset endpoints. Generate a new value;
# never commit the populated .env file.
XLEROBOT_ACT_TOKEN=replace-with-a-long-random-token
