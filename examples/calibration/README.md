# Calibration examples

Small, privacy-reviewed input captures and expected solver results belong here.
They must describe their unit convention and may not contain a private map,
home image, device serial number, or active per-unit calibration bundle.

